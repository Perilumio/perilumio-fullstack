import { AppShell, Lumio } from '@/components/AppShell';
import { getDashboardData } from '@/lib/data';
export default async function ProfilePage(){ const { profile, progress } = await getDashboardData(); return <AppShell><section className="grid grid-2"><div className="card stack"><div className="hero"><div><h1>Profil</h1><p className="muted">Persönlicher Fortschritt mit Lumio-Avatar.</p></div><div className="avatar"><Lumio /></div></div><div><strong>Name:</strong> {profile?.display_name ?? 'Roger'}</div><div><strong>XP:</strong> {profile?.xp ?? 0}</div><div><strong>Level:</strong> {profile?.level ?? 1}</div></div><div className="card stack"><h2>Fortschritt</h2><div><strong>Bestandene Lektionen:</strong> {progress.filter((item: any) => item.passed).length}</div><div><strong>Battlepunkte:</strong> +124</div><div><strong>Freundesrang:</strong> #4</div></div></section></AppShell>; }