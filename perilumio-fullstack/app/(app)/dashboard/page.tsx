import { AppShell, Lumio } from '@/components/AppShell';
import { getDashboardData } from '@/lib/data';
export default async function DashboardPage(){ const { profile, lessons, progress } = await getDashboardData(); const completed = progress.filter((item: any) => item.passed).length; const percent = lessons.length ? Math.round((completed / lessons.length) * 100) : 0; return <AppShell><section className="stack"><div className="card hero"><div><span className="pill">● Live MVP</span><h1>Willkommen zurück, {profile?.display_name ?? 'Lehrling'}.</h1><p className="muted">Lumio begleitet deinen Lernfortschritt im schwarz-neonblauen App-Look.</p></div><Lumio /></div><div className="grid grid-4"><div className="card"><div className="muted">XP</div><div className="kpi">{profile?.xp ?? 0}</div></div><div className="card"><div className="muted">Level</div><div className="kpi">{profile?.level ?? 1}</div></div><div className="card"><div className="muted">Lernfortschritt</div><div className="kpi">{percent}%</div></div><div className="card"><div className="muted">Lektionen</div><div className="kpi">{completed}/{lessons.length}</div></div></div></section></AppShell>; }